{
  "name": "{{name}}",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "{{homepage}}",
  "author": "{{author}}",
  "license": "MIT",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "{{packageManager}} run type-check && vite build",
    "type-check": "tsc --noEmit",
    "format": "prettier --write \"**/*.{js,ts,tsx,json}\""
  }
}
